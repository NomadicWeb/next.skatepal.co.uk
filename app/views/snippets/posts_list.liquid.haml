{% for post in contents.posts %}
.a-post

  %h3.post-title
    %a{:href => "/posts/{{ post._slug }}"} {{ post.title | escape}}

  %small.post-posted-at {{ post.posted_at | localized_date: %m/%d/%y }}
  %p.post-teaser {{ post.teaser | escape | truncatewords: 30, ""}}
  %small
    %a.post-read-more{:href => "/posts/{{ post._slug }}"} Read the full story
  {% if post.featured_image.url %}
  %img.post-featured-image.img-responsive{:src => "{{ post.featured_image.url }}", :alt => "{{ post.title | escape }}"}
  {% endif %}

{% endfor %}
